apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "omni.fullname" . }}-test-connection"
  labels:
    {{- include "omni.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "omni.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
