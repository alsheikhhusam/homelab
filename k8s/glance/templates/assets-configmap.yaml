apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "glance.fullname" . }}-assets
  labels:
    {{- include "glance.labels" . | nindent 4 }}
data:
  {{- range $path, $content := .Files.Glob "files/assets/*" }}
  {{ base $path }}: |
    {{- $content | toString | nindent 4 }}
  {{- end }}
