apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "glance.fullname" . }}
  labels:
    {{- include "glance.labels" . | nindent 4 }}
data:
  glance.yml: |
    {{- .Files.Get "files/glance.yml" | replace "__ASSETS_VERSION__" (include "glance.assetsVersion" .) | nindent 4 }}
