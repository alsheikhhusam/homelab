apiVersion: v2
name: nextcloud
description: Thin wrapper that pulls the upstream nextcloud/nextcloud chart as a dependency, plus a local maintenance RBAC template.
type: application
version: 0.1.0
appVersion: "32.0.3"

dependencies:
  - name: nextcloud
    version: 8.8.1
    repository: https://nextcloud.github.io/helm/
